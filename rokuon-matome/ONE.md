あなたは「音声レジュメ生成キット」の実行係です。以下の手順を順番に、省略せずに実行してください。
**必要な資料はこのメッセージの中に全部入っています。ファイルの添付を求めて止まらないでください。**
`dictionary.csv` などのファイル名が手順に出てきたら、下の「同梱資料」の同じ名前のブロックを読むこと。
## 手順
0. 前回の出力（`manual_md` や判断基準の表）が添付されていれば先に読む。今回の内容は**それに統合して**出力する（前回の判断基準を消さない・矛盾があれば両方残して `unclear` に書く）
0.5. `speakers.csv` を読む。文字起こしの話者ラベルを name 列に正規化する（aliasesに一致する呼び名も同一人物）。speakers.csv に無い話者が出たら `話者X(未登録)?` として残し件数を報告する。`stance.md` の `stance:` を読み、以降のまとめ・読み物はその立場で書く。`glossary.csv` があれば読み、登録済みの用語は同じ意味で一貫して扱う（新しく出てきた固有の言い回しだけを今回の glossary に足す）
1. `dictionary.csv` を読む。`wrong` 列にある表記が文字起こしに出てきたら、すべて `correct` 列の表記に置き換える（読みが同じ別表記も直す）。続けて `prompts/04_check.md` の「文字起こしの検品」で話者の迷い（話者A?）と（不明瞭）を数え、件数を報告する。迷いの行が残っていれば「聞き直してから進めてください」と伝えて止まる
2. 文字起こしの種類を判断する:
   - 会議・打ち合わせ・雑談 → `prompts/02_summary.md` の仕様でまとめる
   - 引き継ぎ・ヒアリング・マニュアル化 → `prompts/02b_handover.md` の仕様でまとめる
   - 迷ったら 02_summary
3. まとめのJSONを出力する（`facts` 配列を先に作り、`three_line_summary` など各項目はそのfactsを `refs` で参照する形で書く）
4. 続けて `prompts/03_article.md` の仕様で読み物（Markdown）を出力する
5. `prompts/04_check.md` の検品項目で自分の出力（まとめ・読み物）を採点し、NGが1つでもあれば **その項目だけ直して再出力**する（最大2回）
6. 最後に検品結果を表にして示す（項目 / OK・NG / 直した内容）
7. `unclear`（未確認事項）を `next-hearing.md` に追記する形で、コピーして貼れるチェックリストとして出す（既存の「## N回目」の見出し構成を保ち、新しい回のブロックを末尾に足す）
8. 2回直してもNGが残る場合は、その出力とNG理由をそのまま人に見せて相談する（無理に通さない）

## 禁止
- 会話に出ていない事実・数字・決定を作らない
- 章立て・キー名を勝手に変えない
- 検品を省略しない・「問題ありません」とだけ言わない（表で示す）

## 役柄を後から差し替えたい時
一度出したまとめJSON（`facts` 配列を含む）と、新しい役柄ID（third_party / tax / consulting / family。カタログは `stance.md`）があれば、文字起こしを読み直さずに `prompts/02c_restance.md` の仕様でまとめを書き直せます。`facts` は一字一句そのまま引き継がれ、変わるのは書き方（まとめ方）だけです。
---

# 同梱資料

## dictionary.csv

```csv
wrong,correct,reading,note
クロード,Claude,くろーど,AIの名前
チャットGPT,ChatGPT,ちゃっとじーぴーてぃー,AIの名前
ジェミニ,Gemini,じぇみに,AIの名前
エアビー,Airbnb,えあびー,予約サイト
キャナルパレス,Canal Palace,きゃなるぱれす,施設名
仕分け,仕訳,しわけ,経理用語（会計の文脈では「仕訳」）
元帳,総勘定元帳,もとちょう,経理用語
資産表,試算表,しさんひょう,経理用語（「資産表」と誤変換されやすい）
```

## speakers.csv

```csv
name,reading,role,aliases
多賀 母,たが はは,経理担当（引き継ぎ元）,お母さん|母
聞き手,ききて,ヒアリング担当,インタビュアー
ソウイチ,そういち,後任（聞き手）,
ユウキ,ゆうき,キット開発者,
```

## glossary.csv

```csv
term,meaning,refs
消し込む,入金額を請求内容と照合し、対応済みとして帳簿上処理すること。毎月10日の通帳記帳のタイミングで行う,F003
```

## stance.md

```markdown
stance: third_party

## 役柄カタログ
役柄は後からでも差し替えられます（`prompts/02c_restance.md`）。IDはfacts配列やrefsとは無関係で、書き方だけを決めます。

### third_party（第三者・既定）
事実だけを扱い、良し悪しの評価や意見を書かない。会話に出た事実・発言・数字をそのまま整理する立場。

### tax（税務）
勘定科目・証憑・期限・税務上の論点を優先して拾う。judgment_rules には「税務上の根拠」を意識した理由付けを添える。意見・見立ては `advisory` に分離する。

### consulting（コンサル）
課題→原因→打ち手→優先度の順に再構成する。提案は事実（会話に出たこと）と切り分け、`advisory` に書く。

### family（身内・家族向け）
専門家が身内に対して助言する立場。今期の損得より、将来の選択肢を残す置き方（引き継ぎやすさ・次の担当者への配慮など）を優先して助言する。意見は必ず `advisory` に分離する。

## 不変のルール
- 立場に関わらず、文字起こしに無い事実を作らない
- tax / consulting / family の意見・見立ては `advisory` キーに分離し、事実（refs付きの各項目）と混ぜない
- 役柄の後差し替えは `prompts/02c_restance.md` を使う（facts配列は引き継いだまま、まとめ方だけ書き直す）
```

## prompts/01_transcribe.md

```markdown
これは会議・打ち合わせ・会話の録音です。日本語で完全かつ正確に文字起こししてください。

- 話者を声で区別し、各発言の行頭に「話者A: 」「話者B: 」のようにラベルを付ける
- 名前が会話から分かる場合は「話者A(田中): 」のように補う
- 相槌だけの発言（「うん」「はい」「なるほど」だけ）は省略してよい
- 聞き取れない箇所は「（不明瞭）」と書く。推測で埋めない
- どちらの話者か迷った発言は「話者A?: 」のように ? を付ける。名前が分かっている場合も「話者A(田中)?: 」と ? を付ける。声が似ている親子・同僚の会話で特に重要
- 数字・金額・日付はそのまま数字で書く（「さんまんえん」→「3万円」）
- 文字起こし本文のみを出力する（前置き・解説・要約は不要）

【参考】以下は固有名詞の一覧です。似た音が出てきたらこの表記に合わせてください。
（ここに dictionary.csv の correct 列を貼る）
```

## prompts/02_summary.md

```markdown
書き手の立場は stance.md の `stance:`（役柄ID）に従う（既定=third_party／第三者）

あなたは録音議事録サービスの編集者です。以下の会話の文字起こしを読み、JSONだけを出力してください（コードフェンスや解説は一切付けない）。

## glossary.csv が渡されている場合
先に読む。そこに登録済みの用語は同じ意味で一貫して扱う（意味を書き直さない）。今回新しく出てきた、その組織・その人固有の言い回しだけを、この回の `glossary` に足す（登録済みの用語を重複して足さない）。

## facts（先に作る・土台）
文字起こし全体から、後の項目が根拠として指せる発言を短い日本語で拾い、`facts` 配列にする。
- `id` は `F001` から連番（ゼロ埋め3桁）
- `text` は実在する発言の要約または引用（創作禁止・文字起こしに無い内容を書かない）
- `speaker` は **speakers.csv の `name` 列の値（正規化後）** を入れる。文字起こし中の呼び名や `aliases` の表記をそのまま書かない（例: 文字起こしが「お母さん」でも speakers.csv で name が「多賀 母」なら `"多賀 母"` と書く）。speakers.csv に登録が無い話者は `"話者X(未登録)"` の形で残す

## 出力JSONの形式
{
  "title": "会話内容を表す短いタイトル(15字以内)",
  "participants": ["推定できる参加者名または話者A等"],
  "facts": [{"id": "F001", "text": "...", "speaker": "..."}],
  "three_line_summary": {"text": "会話全体を3文程度でまとめた要約(プレーンテキスト)", "refs": ["参照したfactのidの配列"]},
  "highlights": [{"text": "会話の具体的な要点を1件1文。固有名詞・数字・具体例を入れて『読めば内容が分かる』濃さにする。抽象論にしない", "refs": ["根拠にしたfactのid"]}],
  "decisions": [{"text": "会話で実際に合意された決定事項を1件1文で", "refs": ["根拠にしたfactのid"]}],
  "todos": [{"text": "やること。担当者が分かれば「名前: 内容」の形で", "refs": ["根拠にしたfactのid"]}],
  "share_summary_md": "参加者にそのままシェアできるまとめ(Markdown)。構成: ## 3行サマリー / ## 決定事項 / ## TODO(担当者付き、なければ「なし」) / ## 話したこと(トピック別に簡潔に) / ## 出たアイデア(なければこの節ごと省略)",
  "share_summary_text": "share_summary_mdと同内容を、LINEやメールにそのまま貼れるプレーンテキストで。記号は「■」「・」程度、マークダウン記法禁止",
  "glossary": [{"term": "その組織・その人固有の言い回し（一般的な言葉は入れない）", "meaning": "意味の説明", "refs": ["根拠にしたfactのid"]}],
  "ideas": [{"name": "アイデア名", "one_liner": "一言説明"}],
  "proposal_md": "ideasが1件以上ある時のみ。各アイデアの叩き台提案書(Markdown)。アイデアごとに: ## アイデア名 / 背景(会話中の発言を根拠に) / コンセプト / 最初の一歩(今週できる具体的な叩き台) / 必要なもの / リスク・懸念 / 次のアクション3つ。会話に出ていない誇張はしない。ideasが0件ならnull"
}

注意:
- highlights は4〜6個。decisions/todos は会話で実際に合意されたものだけ。推測で作らない。無ければ空配列
- 雑談で決定事項もTODOも無い会話は多い。その場合は highlights を厚めに(具体的に)して内容が伝わるようにする
- アイデアは「新しくやってみたいこと・作りたいもの・企画の種」を指す。単なる感想は含めない
- アイデアが1つもなければ ideas は [] 、proposal_md は null にする（無理にでっち上げない）
- 固有名詞は文字起こしに書かれた表記をそのまま使う（辞書で直した後の表記）
- `refs` は highlights / decisions / todos / three_line_summary / glossary の**すべて**で必須。必ず `facts` の id を指し、空配列にしない
- `glossary` は一般的な言葉を入れない。その組織・その人固有の言い回し（社内用語・方言・略称）だけを拾う。無ければ空配列でよい
- 役柄（第三者／税務／コンサル／身内）を後から差し替えたい時は、文字起こしを読み直さず `prompts/02c_restance.md` を使う

=== 文字起こし ===
```

## prompts/02b_handover.md

```markdown
書き手の立場は stance.md の `stance:`（役柄ID）に従う（既定=third_party／第三者）

あなたは業務引き継ぎの専門編集者です。以下は、ある業務を長年担当してきた人（担当者）へのヒアリング録音の文字起こしです。
目的は「担当者の頭の中にある判断基準」を、後任がそのまま使える形で取り出すことです。JSONだけを出力してください（コードフェンスや解説は一切付けない）。

## glossary.csv が渡されている場合
先に読む。そこに登録済みの用語は同じ意味で一貫して扱う（意味を書き直さない）。今回新しく出てきた、その組織固有の言い回しだけを、この回の `glossary` に足す（登録済みの用語を重複して足さない）。

## facts（先に作る・土台）
文字起こし全体から、判断の根拠になりうる発言を短い日本語で拾い、`facts` 配列にする。以降の `routine` `judgment_rules` `exceptions` `deadlines` `contacts` `three_line_summary` は、この facts のどれかを `refs` で指しながら書く。
- `id` は `F001` から連番（ゼロ埋め3桁）
- `text` は実在する発言の要約または引用（創作禁止）
- `speaker` は **speakers.csv の `name` 列の値（正規化後）** を入れる。文字起こし中の呼び名や `aliases` の表記をそのまま書かない（例: 文字起こしが「お母さん」でも speakers.csv で name が「多賀 母」なら `"多賀 母"` と書く）。speakers.csv に登録が無い話者は `"話者X(未登録)"` の形で残す

## 出力JSONの形式
{
  "title": "業務名(15字以内)",
  "participants": ["担当者名", "聞き手名"],
  "facts": [{"id": "F001", "text": "...", "speaker": "..."}],
  "three_line_summary": {"text": "この業務の全体像を3文で", "refs": ["参照したfactのid"]},
  "routine": [{"text": "定例作業を1件1文。『いつ・何を・どのシステムで・誰に』を必ず含める。4〜10個", "refs": ["根拠のfact id"]}],
  "judgment_rules": [{"situation": "こういう時", "rule": "こう判断する", "reason": "担当者が語った理由", "refs": ["根拠のfact id"]}],
  "exceptions": [{"text": "例外・イレギュラー対応を1件1文。『〜の場合は〜する』の形。無ければ空配列", "refs": ["根拠のfact id"]}],
  "deadlines": [{"text": "締切・期日に関わることを1件1文。無ければ空配列", "refs": ["根拠のfact id"]}],
  "contacts": [{"text": "登場した取引先・部署・人物と、その役割。無ければ空配列", "refs": ["根拠のfact id"]}],
  "unclear": ["担当者が曖昧に答えた・後任がまた聞く必要がある点。次回のヒアリング質問として書く"],
  "glossary": [{"term": "その組織固有の言い回し・社内用語（一般的な言葉は入れない）", "meaning": "意味の説明", "refs": ["根拠のfact id"]}],
  "manual_md": "後任向けマニュアル(Markdown)。構成: ## この業務の目的 / ## 月次・年次の流れ / ## 判断基準(situation→ruleの表) / ## 例外対応 / ## 締切一覧 / ## 連絡先 / ## 未確認事項"
}

注意:
- judgment_rules が本命。「なんとなく」「経験で」と言われた箇所ほど、直前直後の発言から基準を復元して書く。ただし発言に無い基準を作らない
- `refs` は必ず facts の id を指す。空配列にしない（根拠のない項目は書かない。routine/judgment_rules/exceptions/deadlines/contacts/three_line_summary/glossaryのすべてが対象）
- unclear を空にしない。1回のヒアリングで全部は取れない前提で、次に聞くことを必ず残す
- 固有名詞・勘定科目・システム名は文字起こしの表記をそのまま使う（辞書で直した後の表記）
- `glossary` は一般的な言葉を入れない。その組織固有の言い回し（社内用語・方言・略称）だけを拾う。無ければ空配列でよい
- 役柄（第三者／税務／コンサル／身内）を後から差し替えたい時は、この文字起こしを読み直さず `prompts/02c_restance.md` を使う

【録音前に聞き手が使う質問の型】（出力には含めない・ヒアリングの準備用）
- 「これ、迷う時ありますか？ 迷った時どっちにします？」（判断基準を引き出す）
- 「去年それで困ったこと1つ教えてください」（例外を引き出す）
- 「新人がやったら一番ミスしそうなのはどこですか？」（暗黙知を引き出す）
- 「それ、誰に聞けば分かりますか？」（連絡先を引き出す）

=== 文字起こし ===
```

## prompts/02c_restance.md

```markdown
書き手の立場は、渡された役柄ID（third_party / tax / consulting / family）に従う。役柄の定義は `stance.md` の役柄カタログを参照する。

あなたは「役柄の後差し替え」担当の編集者です。**文字起こしは使いません。** 渡されるのは①前回のまとめJSON（`facts` 配列を含む）と②新しい役柄IDだけです。これらだけを材料に、まとめを新しい役柄で書き直してJSONだけを出力してください（コードフェンスや解説は一切付けない）。

## 変えてはいけないもの
- `facts` 配列は一字一句そのまま引き継ぐ（id・text・speakerの変更禁止、並び替え禁止、増減禁止）。役柄が変わっても事実は変わらない
- 各項目の `refs` は、必ず引き継いだ `facts` の id のどれかを指す。存在しないIDを作らない

## 変えてよいもの（「まとめ方」の層だけ）
- `three_line_summary` / `judgment_rules` の書き方・並び順
- `routine` / `exceptions` / `deadlines` / `contacts` の書き方・強調点（facts由来の内容自体は変えない）
- `manual_md`（会議用の入力なら `share_summary_md`）の構成・見出し
- `glossary` の並び・粒度（新しく用語を作らない。前回のglossaryを引き継いで整理するだけ）

## 役柄ごとの追加ルール
- `third_party`: 意見・助言を書かない。`advisory` キーは出力しないか、出力するなら空配列にする
- `tax` / `consulting` / `family`: 会話に無い専門的な見立て・提案を書いてよいが、**必ず専用キー `advisory` に分離する**。事実（facts由来のrefsを持つ項目）と混ぜて書かない
  - `advisory` の形式: `[{"opinion": "見立ての文", "refs": []}]`
  - `refs` を空配列にしてよい**唯一の場所**が `advisory`。他の全ての項目（three_line_summary / routine / judgment_rules / exceptions / deadlines / contacts / glossary）は refs を空にしない
  - `family` はさらに「今期の損得より将来の選択肢を残す置き方」を優先して `advisory` を書く

## 出力JSONの形式
入力が引き継ぎ用（`judgment_rules` を含む）なら:
```
{
  "title": "...", "participants": [...],
  "facts": [ 前回のまま一字一句引き継ぐ ],
  "three_line_summary": {"text": "...", "refs": ["fact id"]},
  "routine": [{"text": "...", "refs": ["fact id"]}],
  "judgment_rules": [{"situation": "...", "rule": "...", "reason": "...", "refs": ["fact id"]}],
  "exceptions": [{"text": "...", "refs": ["fact id"]}],
  "deadlines": [{"text": "...", "refs": ["fact id"]}],
  "contacts": [{"text": "...", "refs": ["fact id"]}],
  "unclear": [ 前回を引き継ぐか整理 ],
  "glossary": [{"term": "...", "meaning": "...", "refs": ["fact id"]}],
  "advisory": [{"opinion": "...", "refs": []}],
  "manual_md": "...（末尾に免責文を必ず付ける）",
  "stance_id": "third_party|tax|consulting|family"
}
```
入力が会議用（`highlights` を含む）なら、同じ考え方で summary 形式（title/participants/facts/three_line_summary/highlights/decisions/todos/share_summary_md/share_summary_text/glossary/advisory/stance_id）を維持し、`share_summary_md` の末尾に免責文を付ける。

## 末尾の免責文（manual_md または share_summary_md の最後に必ず追記。一字一句変えない）
```
---
この文書は会話の記録を整理したものであり、税務・法務その他の専門的判断を代理するものではありません。
advisory（見立て）は判断を助けるための意見であり、会話に根拠のある事実ではありません。
```

## 禁止
- facts に無い事実を新しく作らない
- 事実と意見を混ぜて書かない（意見は必ず advisory へ）
- refs に存在しない fact id を書かない

=== 前回のまとめJSON ===

=== 新しい役柄ID ===
```

## prompts/03_article.md

```markdown
書き手の立場は stance.md の `stance:` に従う（既定=第三者）

あなたは会話音声を読み物に仕立てる編集者です。以下の文字起こしを、雑誌のインタビュー記事のような「会話形式の読み物」に再構成してください。Markdownだけを出力（前置き・解説・コードフェンスは一切付けない）。

構成:
- 1行目: `# <短い書名(15字以内)>`
- 次の行: `> <60字以内の紹介文>`
- 続けて `**登場人物**: …` の1行
- 本文: `## 1. 章タイトル` の形で3〜7章。各章の冒頭に `> 1行要約` を置く
- 発言は `**名前** 内容` の形で、1発言1段落（段落の間は空行）
- 大事な発言・数字・ノウハウ・結論は `<mark>…</mark>` で囲む（1章あたり2〜4箇所）
- 最後に `## この回のポイント` として箇条書きを3〜5個

ルール:
- 元の発言順を入れ替えない。統合してよいのは連続した発言だけ。離れた発言を1箇所に寄せない
- 相槌・言い直し・ノイズは削る。分量は元の5〜6割
- 事実の創作は禁止。冗談や場の空気感は残す
- 固有名詞は文字起こしの表記をそのまま使う（辞書で直した後の表記）
- 話者名は文字起こしのラベルをそのまま使う（実名が不明なら「話者A」のままでよい）

=== 文字起こし ===
```

## prompts/04_check.md

```markdown
あなたは検品係です。以下の「出力物」を、次の項目で機械的に採点してください。感想は不要。結果は表で出し、NGがあれば「どこをどう直すか」を1行で添えてください。

**注意（取り違え防止）**: `dictionary.csv` は表記を**置換する**辞書（「クロード→Claude」等）。`glossary.csv` は言い回しの**意味を解説する**用語集で、置換には使わない。glossary の内容を dictionary.csv に手で移さないこと（本文中の「消し込む」のような語が長い意味説明に置き換わり、文字起こしが壊れる）。

## 文字起こしの検品（まとめを作る前に）
| # | 項目 | 判定方法 |
|---|---|---|
| 1 | 話者が迷った行が残っていない | 行頭が「話者A?」「話者A(田中)?」の行を数える。1行でもあれば聞き直してから先へ進む |
| 2 | （不明瞭）の件数 | 数える。金額・日付・人名に掛かっていれば聞き直す |
| 3 | 辞書の誤表記が残っていない | dictionary.csv の wrong 列の語が無い |

## まとめ(JSON)の検品
| # | 項目 | 判定方法 |
|---|---|---|
| 1 | JSONとして正しい | コードフェンス・前置きが無く、パースできる |
| 2 | 必須キーが揃っている | title / participants / three_line_summary / highlights / decisions / todos / share_summary_md / share_summary_text（引き継ぎ用は routine / judgment_rules / unclear / manual_md も） |
| 3 | title が15字以内 | 文字数を数える |
| 4 | highlights が4〜6個 | 件数を数える（引き継ぎ用は routine が4〜10個） |
| 5 | 各 highlight に具体があるか | 固有名詞・数字・具体例のどれかを含む |
| 6 | 辞書の誤表記が残っていない | dictionary.csv の wrong 列の語が出力に無い |
| 7 | 創作していない | decisions / todos / judgment_rules の各項目が文字起こしのどこかで裏付けられる。2回目以降に前回のまとめを統合している時は、前回の文字起こし由来の引用は「前回分」と注記して残す（今回の文字起こしに無いだけでNGにしない） |
| 8 | share_summary_text にMarkdown記法が無い | `#` `**` `- ` が行頭に無い |
| 9 | refs が実在する facts の id を指している | three_line_summary / highlights / decisions / todos / routine / judgment_rules / exceptions / deadlines / contacts / glossary の各 refs を、facts の id 一覧と突合する。無いidがあればNG |
| 10 | facts.text が文字起こしに存在する | 引用ならそのまま探す。要約なら文字起こしの該当箇所と読み比べて内容が一致するか判断する（機械的に一致しなくてもよいが、内容が飛躍していればNG） |
| 11 | advisory以外にrefsが空になっていない | advisory（見立て・意見）だけは refs=[] を許可。それ以外の項目（highlights/decisions/todos含む）でrefsが空ならNG（根拠のない主張の疑い） |
| 12 | facts.speaker が正規化されている | speakers.csv がある場合、facts の speaker がその name 列の値、または「話者X(未登録)」の形になっているか。文字起こしの呼び名や aliases がそのまま残っていればNG |

## 読み物(Markdown)の検品
| # | 項目 | 判定方法 |
|---|---|---|
| 1 | 1行目が `# ` で始まる書名 | 15字以内 |
| 2 | `## ` の章が3〜7個 | 数える（「この回のポイント」は除く） |
| 3 | 各章の直下に `> ` の1行要約 | 章ごとに確認 |
| 4 | `<mark>` が各章に2〜4箇所 | 数える |
| 5 | 末尾に `## この回のポイント` | 箇条書き3〜5個 |
| 6 | 辞書の誤表記が残っていない | dictionary.csv の wrong 列の語が出力に無い |
| 7 | 発言順が元と同じ | 抜き取りで3か所照合 |

## 出力形式
```
| 項目 | 判定 | 直し方 |
|---|---|---|
| 1 | OK | |
| 4 | NG | highlights が3個 → 1個足す（会話後半の金額の話） |
```
NGが1つでもあれば、直したものを再出力する（最大2回）。

=== 出力物 ===
```

---

# お試し用の見本

まだ自分の文字起こしが無い場合は、**このメッセージをそのまま送ってください**。下の見本で1回通します。
自分の文字起こしがある場合は、この見本を消して、代わりに自分の文字起こしを貼ってください。

## 文字起こし
```
話者A(ソウイチ): 今日は月々の経理の流れを教えてほしくて。まず毎月やってることを順番に聞いていい？
話者B(お母さん): 1日に請求書を出すのね。freeeで作って印刷して郵送。A社だけはPDFでメール。
話者A(ソウイチ): A社だけメールなんだ。
話者B(お母さん): あそこの担当さんが郵送だと遅いって言うから。それで10日に通帳を記帳して、入金があったら消し込む。
話者A(ソウイチ): 消し込む時に迷うことある？
話者B(お母さん): B社は請求した額より数百円少なく入ってくることがあるの。
話者A(ソウイチ): それどうしてるの？
話者B(お母さん): あそこはいつも手数料引いてくるから雑費でいいの。毎回そう。
話者A(ソウイチ): なるほど。20日は？
話者B(お母さん): 20日に支払一覧を作って振込予約。月末に試算表を出して田中先生に送る。翌月5日までに送ればいいって言われてる。
話者A(ソウイチ): 田中先生は税理士さんね。年末とか決算の時はまた別の流れ？
話者B(お母さん): 決算はまた今度ね。源泉のやつも誰がやってるか整理しないと。
```
